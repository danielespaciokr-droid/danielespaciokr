Pillow>=8.0
pywin32>=300; sys_platform == "win32"
# Optional: preview iPhone HEIC photos (remove the leading "# " to install it).
# Keep this file ASCII-only: older pip versions read it with the Windows code page.
# pillow-heif>=0.10
